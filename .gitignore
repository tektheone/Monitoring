# Go Backend
# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib
fleet-management-server
device-simulator-*
run-server
server

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool
*.out

# Dependency directories (remove the comment below to include it)
# vendor/

# Go workspace file
go.work

# Results and logs
results.txt
*.log

# React Frontend
# Dependencies
node_modules/
.pnp
.pnp.js

# Production builds
dist/

# NPM/Yarn lockfiles (requested to ignore)
package-lock.json
yarn.lock
pnpm-lock.yaml
/frontend/dist/
/frontend/build/

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Coverage directory used by tools like istanbul
coverage/
*.lcov

# nyc test coverage
.nyc_output

# Vite
.vite/

# Tailwind CSS
# (Usually no specific ignores needed, but sometimes temp files)

# Shadcn UI
# (Component registry and temp files)
components.json.bak

# IDE and Editor files
.vscode/
.idea/
*.swp
*.swo
*~

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Temporary files
*.tmp
*.temp
.cache/

# Package manager lock files (choose one strategy)
# Option 1: Include lock files (recommended for applications)
# package-lock.json
# yarn.lock
# pnpm-lock.yaml

# Option 2: Exclude lock files (for libraries)
# Uncomment if this is a library rather than an application
# package-lock.json
# yarn.lock
# pnpm-lock.yaml